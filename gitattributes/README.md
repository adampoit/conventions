# gitattributes

Installs a standard `.gitattributes` file for consistent line endings and diff behavior.

## Settings

None.

## Behavior

- Creates `.gitattributes` if it doesn't exist
- Overwrites existing `.gitattributes` with the published standard
